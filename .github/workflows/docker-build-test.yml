name: Test Docker Build

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build-test:
    name: Test Docker build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.pi
          push: false
          load: true
          tags: pi-agent:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Validate image
        run: |
          docker inspect pi-agent:test
          docker run --rm pi-agent:test pi --version || true
